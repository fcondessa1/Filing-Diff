# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Secrets — never commit an API key
.env
*.key

# Cached filings: large, regenerable, and they bloat the repo fast.
# Commit ONE small sample filing instead so the repo is runnable.
data/
filings/
*.html

# OS / editor
.DS_Store
.vscode/
.idea/
